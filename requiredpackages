
Install yq

wget https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -O /usr/local/bin/yq &&\
    chmod +x /usr/local/bin/yq

===========
Install docker

# Add Docker's official GPG key:
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update

sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

sudo systemctl status docker
sudo systemctl start docker

=========================


Dockerfile sample

FROM alpine:3.20
# Copy your file into the image (e.g. to /tmp)
COPY myfile.txt /tmp/myfile.txt
# Optional: ensure coreutils are present (sleep is in busybox by default)
# RUN apk add --no-cache coreutils
# Sleep 3600 seconds (1 hour) as the default entrypoint
ENTRYPOINT ["sleep", "3600"]


Build Docker image
docker build -t msuranagi/poc:tgz-new .

Run docker image. -- to test the image
docker run -d --name poc_container msuranagi/poc:tgz-new

perform docker login with a token

Push docker image -- ensure the repo exists in docker hub
docker push msuranagi/poc:tgz-new
